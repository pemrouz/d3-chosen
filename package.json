{
  "name": "d3-chosen",
  "version": "0.0.0",
  "main": "dist",
  "author": "Pedram Emrouznejad (https://github.com/pemrouz)",
  "license": "pemrouz.mit-license.org",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pemrouz/d3-chosen.git"
  },
  "scripts": {
    "bundle": "browserify dist/index.js > dist/client.js -s chosen",
    "babel": "babel src -d dist --copy-files",
    "clean": "rm -rf dist && mkdir dist",
    "export": "sh -c 'rijs.export dist'",
    "build": "npm run clean && npm run babel && npm run export && npm run bundle",
    "test": "popper",
    "version": "npm run build && git add -A",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "babel-cli": "*",
    "babel-preset-es2015": "*",
    "babelify": "*",
    "popper": "*",
    "browserify": "*",
    "browserenv": "*",
    "tape": "*",
    "utilise": "*",
    "cssscope": "*",
    "utilise.filify": "*",
    "rijs.export": "*"
  }
}
